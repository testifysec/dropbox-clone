apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dropbox-clone

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/part-of: dropbox-clone
